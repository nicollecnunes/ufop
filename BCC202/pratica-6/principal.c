#include "pilha.h"
#include <stdio.h>
#include <string.h>

int main()
{
        Pilha pilha;
        char ch;
        Item item;

        Pilha_Inicia(&pilha);

        while (scanf("%c", &ch)!=EOF){
          //processar cada elemento lido
        }

        //determinar o resultado

        Pilha_Esvazia(&pilha);

        return 0;//nao remova
}
